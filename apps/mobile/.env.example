# Base URL of the Sensei API.
# On a simulator, localhost works. On a physical device, use your machine's
# LAN IP (e.g. http://192.168.1.20:3000) so the phone can reach the API.
EXPO_PUBLIC_API_URL=http://localhost:3000
